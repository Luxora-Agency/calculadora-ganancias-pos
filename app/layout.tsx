import type { Metadata, Viewport } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Calculadora de Ganancias - Sistema POS Multi-Nicho",
  description: "Simula y proyecta tus ganancias potenciales como vendedor del Sistema POS Multi-Nicho. Calcula comisiones según tu rol y equipo.",
  keywords: ["calculadora", "ganancias", "POS", "comisiones", "ventas", "multinivel"],
  authors: [{ name: "Sistema POS Multi-Nicho" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        {children}
      </body>
    </html>
  );
}
